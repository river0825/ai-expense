# Tasks: Detect Payment Account

## Backend (TDD)
- [x] Create DB migration for `payment_method` column (Postgres) @backend
- [x] Update `Expense` and `ParsedExpense` domain models in Go @backend
- [x] **TEST**: Write failing test for `GeminiAI` payment method extraction @backend
- [x] Update `GeminiAI` prompt and parsing logic to extract payment method @backend
- [x] **TEST**: Write failing test for `ParseConversationUseCase` default logic @backend
- [x] Update `ParseConversationUseCase` to handle default "Cash" logic @backend
- [x] **TEST**: Write failing integration test for `ExpenseRepository` (Postgres) @backend
- [x] Update `ExpenseRepository` (Postgres) to handle new column @backend

## Frontend (Mobile First)
- [x] Update Frontend `Expense` model @frontend
- [x] Apply `ui-ux-pro-max` design tokens (colors, fonts) @frontend
- [x] Update `ExpenseList` UI to display Payment Method (Mobile optimized) @frontend

