# AI Expense Tracker - Local Environment Configuration
# Copy this file to .env.local and update with your values

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Option 1: Use SQLite for simple local development (default)
# DATABASE_PATH=./aiexpense.db

# Option 2: Use PostgreSQL/Supabase for realistic local testing
# First, install Supabase CLI: brew install supabase/tap/supabase
# Then run: supabase start
# Extract connection string: supabase status
# DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# =============================================================================
# AI PROVIDER CONFIGURATION
# =============================================================================

# AI Provider: "gemini" or "openai"
AI_PROVIDER=gemini

# Google Gemini API Key (required if AI_PROVIDER=gemini)
GEMINI_API_KEY=your-gemini-api-key-here

# OpenAI API Key (required if AI_PROVIDER=openai)
# OPENAI_API_KEY=your-openai-api-key-here

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server port
SERVER_PORT=8080

# Server environment: "development" or "production"
# In development mode, the server logs more details and allows additional debugging endpoints
SERVER_ENV=development

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level: "debug", "info", "warn", "error"
LOG_LEVEL=debug

# =============================================================================
# NOTES FOR LOCAL DEVELOPMENT
# =============================================================================

# For SQLite (simpler setup):
#   1. Leave DATABASE_PATH=./aiexpense.db (default)
#   2. Migrations run automatically on app startup
#   3. Database file is created in the project root

# For PostgreSQL with Supabase (realistic production testing):
#   1. Install Supabase CLI: brew install supabase/tap/supabase
#   2. Start local instance: supabase start
#   3. Wait for "Local development started"
#   4. Get connection details: supabase status
#   5. Copy DATABASE_URL and paste in this file
#   6. Run app: go run ./cmd/server/main.go
#   7. Migrations run automatically against PostgreSQL
#   8. Stop Supabase when done: supabase stop

# For CI/CD environments, set these variables via your deployment platform:
#   - DATABASE_URL (for production PostgreSQL/Supabase)
#   - GEMINI_API_KEY or OPENAI_API_KEY
#   - SERVER_PORT (optional, defaults to 8080)
